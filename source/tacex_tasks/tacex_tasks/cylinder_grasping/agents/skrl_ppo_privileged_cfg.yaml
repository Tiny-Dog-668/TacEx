seed: 42


# Models are instantiated using skrl's model instantiator utility
# https://skrl.readthedocs.io/en/latest/api/utils/model_instantiators.html
models:
  separate: True
  policy:  # see gaussian_model parameters
    class: GaussianMixin
    clip_actions: False
    clip_log_std: True
    min_log_std: -20.0
    max_log_std: 2.0
    initial_log_std: 0.0
    network:
      - name: net
        input: concatenate([STATES["proprio_obs"], STATES["cylinder_pos"], STATES["cylinder_quat"], STATES["cylinder_lin_vel"], STATES["cylinder_ang_vel"], STATES["gripper_pos"], STATES["gripper_quat"], STATES["gripper_lin_vel"], STATES["gripper_ang_vel"], STATES["target_pos"], STATES["target_distance"], STATES["gripper_state"], STATES["action_history"]])
        layers: [512, 256, 128, 64]
        activations: elu
    output: ACTIONS
  value:  # see deterministic_model parameters
    class: DeterministicMixin
    clip_actions: False
    network:
      - name: net
        input: concatenate([STATES["proprio_obs"], STATES["cylinder_pos"], STATES["cylinder_quat"], STATES["cylinder_lin_vel"], STATES["cylinder_ang_vel"], STATES["gripper_pos"], STATES["gripper_quat"], STATES["gripper_lin_vel"], STATES["gripper_ang_vel"], STATES["target_pos"], STATES["target_distance"], STATES["gripper_state"], STATES["action_history"]])
        layers: [512, 256, 128, 64]
        activations: elu
    output: ONE

# Rollout memory
# https://skrl.readthedocs.io/en/latest/api/memories/random.html
memory:
  class: RandomMemory
  memory_size: -1  # automatically determined (same as agent:rollouts)


# PPO agent configuration (field names are from PPO_DEFAULT_CONFIG)
# https://skrl.readthedocs.io/en/latest/api/agents/ppo.html
agent:
  class: PPO
  rollouts: 512
  learning_epochs: 8 
  mini_batches: 8
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 5.0e-05
  policy_learning_rate: 5.0e-05
  value_learning_rate: 1.5e-04
  learning_rate_scheduler: null
  learning_rate_scheduler_kwargs: null
  state_preprocessor: RunningStandardScaler
  state_preprocessor_kwargs: null
  value_preprocessor: RunningStandardScaler
  value_preprocessor_kwargs: null
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.2
  value_clip: 0.4
  clip_predicted_values: True
  entropy_loss_scale: 0.01
  value_loss_scale: 5.0
  kl_threshold: 0.02
  rewards_shaper_scale: 1.0
  time_limit_bootstrap: True
  # logging and checkpoint
  experiment:
    directory: "cylinder_grasping"
    experiment_name: "privileged"
    write_interval: 1000
    checkpoint_interval: 10000
    wandb: False             # whether to use Weights & Biases
    wandb_kwargs: {
      "sync_tensorboard": True,
      "project": "cylinder_grasping_privileged"
    }


# Sequential trainer
# https://skrl.readthedocs.io/en/latest/api/trainers/sequential.html
trainer:
  class: SequentialTrainer
  timesteps: 1000000
  environment_info: log
